/**
 * Signed-in home: runner portfolio / Strava highlights (nav label “Overview”).
 * Public profile remains `/{runnerDisplayName}` — never use this path as a Profile link.
 */
export const OVERVIEW_PATH = "/dashboard";
